package textinputgroup

// from file "react-core/src/components/TextInputGroup/TextInputGroup.tsx"

type Props struct {
	// Children - Content rendered inside the text input group. Optional.
	Children any // React.ReactNode 
	// ClassName - Additional classes applied to the text input group container. Optional.
	ClassName string
	// IsDisabled - Adds disabled styling and a disabled context value which text input group main hooks into
	// for the input itself. Optional.
	IsDisabled bool
}
