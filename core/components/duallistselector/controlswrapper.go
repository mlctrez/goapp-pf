package duallistselector

// from file "react-core/src/components/DualListSelector/DualListSelectorControlsWrapper.tsx"

type ControlsWrapperProps struct {
	// Children - Anything that can be rendered inside of the wrapper. Optional.
	Children any // React.ReactNode 
	// ClassName - Additional classes added to the wrapper. Optional.
	ClassName string
	// InnerRef - Optional.
	InnerRef any // React.RefObject 
	// AriaLabel - Accessible label for the dual list selector controls wrapper. Optional.
	AriaLabel string
}
